% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Surv_quantile.R
\name{Surv_quantile}
\alias{Surv_quantile}
\title{Weighted quantiles for right-censored data}
\usage{
Surv_quantile(
  censored_data,
  percentiles = c(0.05, 0.1, 0.25, 0.5, 0.75, 0.9, 0.95),
  type = "interp",
  sig.fig = 3
)
}
\arguments{
\item{censored_data}{A data frame whose first three columns are
(1) the measured concentration, (2) a censoring flag, and
(3) a unique site identifier.}

\item{percentiles}{Numeric vector of desired percentiles in \code{(0, 1)}.}

\item{type}{Method for quantile estimation: \code{"interp"} (linear
interpolation between nearest points), \code{"PiR"} (largest order
statistic that does not exceed the percentile), or \code{"nearest"}
(nearest order statistic, possibly exceeding the percentile).}

\item{sig.fig}{Number of significant figures for the estimate.}
}
\value{
A tibble with columns \code{Percentile} and \code{Xh} arranged by
descending percentile.
}
\description{
Calculates the AKME for censored data and returns a table of desired
quantiles. Output contains the desired percentiles and the corresponding
order-statistic estimates. Warnings are emitted if a desired percentile
falls below the censored fraction (which may produce \code{NA} values
depending on \code{type}).
}
\examples{
set.seed(1)
dat <- data.frame(
  Concentration = rlnorm(200, 1, 1),
  Censored = rbinom(200, 1, 0.2),
  Site = sample(letters[1:3], 200, replace = TRUE)
)
Surv_quantile(dat)
}
