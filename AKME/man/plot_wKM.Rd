% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_wKM.R
\name{plot_wKM}
\alias{plot_wKM}
\title{Plot weighted Kaplan-Meier survival curves}
\usage{
plot_wKM(
  grouped_km,
  log_scale = TRUE,
  n_ticks = 5,
  tick_step = 1,
  units = "ppb"
)
}
\arguments{
\item{grouped_km}{Output of \code{\link[=group_km]{group_km()}} (or any tibble with columns
\code{Concentration}, \code{S}, and \code{Dataset}).}

\item{log_scale}{Logical. Use a base-10 log x-axis (default) or
linear.}

\item{n_ticks}{Approximate number of x-axis breaks.}

\item{tick_step}{Retained for backward compatibility; not used.}

\item{units}{Concentration units to display in the x-axis label.}
}
\value{
A \code{ggplot} object.
}
\description{
Returns a ggplot object showing one stepped survival curve per level
of \code{Dataset}. The function is a thin wrapper around \code{ggplot2}.
}
\details{
Note: this function returns the plot rather than drawing it as a side
effect. Top-level callers (the R console, vignettes, knitr chunks)
will auto-print the result; scripts that previously relied on the
side effect must now wrap the call in \code{\link[=print]{print()}}.
}
