% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Surv_weighted.R
\name{Surv_weighted}
\alias{Surv_weighted}
\title{Adjusted (weighted) Kaplan-Meier estimator of the survival function}
\usage{
Surv_weighted(censored_data)
}
\arguments{
\item{censored_data}{A data frame whose first three columns are
(1) the measured concentration, (2) a censoring/detection flag where
\code{1} indicates a non-detect, and (3) a unique site identifier.}
}
\value{
A tibble with columns \code{Concentration}, \code{Site}, \code{weight}, \code{Yw},
\code{dw}, \code{P}, and \code{S} (the survival estimator).
}
\description{
Calculates the adjusted Kaplan-Meier estimator (AKME) of the survival
function for right-censored concentration data, returning the estimator
\code{S} along with the weighted "at risk" (\code{Yw}) and "event" (\code{dw}) values
at each observed (uncensored) concentration. Follows the methodology of
Xie and Liu (2005).
}
\examples{
set.seed(1)
dat <- data.frame(
  Concentration = rlnorm(50, 1, 1),
  Censored = rbinom(50, 1, 0.2),
  Site = sample(letters[1:3], 50, replace = TRUE)
)
Surv_weighted(dat)
}
\seealso{
\code{\link[=Surv_wconf]{Surv_wconf()}} for confidence bands, \code{\link[=Surv_quantile]{Surv_quantile()}} for
quantile estimates, \code{\link[=group_km]{group_km()}} for per-group AKME.
}
