% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_input.R
\name{check_input}
\alias{check_input}
\title{Check inputs to Kaplan-Meier estimator functions}
\usage{
check_input(censored_data)
}
\arguments{
\item{censored_data}{A data frame of (at least) three columns.}
}
\value{
\code{censored_data}, returned invisibly. Called for its side effect of
raising an informative error when the input is malformed.
}
\description{
Validates that \code{censored_data} is a three-column data frame whose columns
hold (1) the measured concentration, (2) a censoring/detection flag, and
(3) a site identifier, in that order.
}
\examples{
good <- data.frame(
  Concentration = rlnorm(100, 1, 2),
  Censored = rbinom(100, 1, 0.2),
  Site = sample(letters[1:3], 100, replace = TRUE)
)
check_input(good)
}
