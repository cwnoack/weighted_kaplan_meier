% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Surv_wconf.R
\name{Surv_wconf}
\alias{Surv_wconf}
\title{Greenwood-style confidence band for the weighted Kaplan-Meier estimator}
\usage{
Surv_wconf(wKM_object, alpha = 0.05)
}
\arguments{
\item{wKM_object}{Output of \code{\link[=Surv_weighted]{Surv_weighted()}}.}

\item{alpha}{Type-I error rate. Default \code{0.05} for a 95\% interval.}
}
\value{
A tibble with the columns of \code{wKM_object} plus \code{std_err}, \code{UCI},
and \code{LCI}.
}
\description{
Adds Greenwood standard errors and approximate \code{(1 - alpha)} confidence
bands to the output of \code{\link[=Surv_weighted]{Surv_weighted()}}.
}
\examples{
set.seed(1)
dat <- data.frame(
  Concentration = rlnorm(50, 1, 1),
  Censored = rbinom(50, 1, 0.2),
  Site = sample(letters[1:3], 50, replace = TRUE)
)
Surv_wconf(Surv_weighted(dat))
}
